# 签到运营平台

本项目用于集中管理外部站点的签到账号、签到操作和签到结果，帮助管理员高效完成日常签到运营并及时处理异常。

## 用户与账号

**管理员**：负责配置平台、管理用户和签到账号，并处理签到运营中的异常。
_避免_：运营者（除非特指组织职责，而不是平台角色）

**普通用户**：只能管理和查看自己拥有的签到账号及其相关签到信息的平台用户。
_避免_：访客（普通用户仍然是已登录的平台用户）

**签到账号**：属于外部签到站点、由平台代为执行签到的账号身份。
_避免_：用户账号、平台账号（这两个词容易与平台用户混淆）

## 签到运营

**签到工作台**：管理员集中查看签到账号状态、发起签到操作、跟踪签到结果并处理失败情况的工作区域。
_避免_：首页、控制台（它们描述的是界面位置，不是业务工作区域）

**签到批次**：一次由管理员或自动调度发起的、针对一个或多个签到账号的统一签到操作集合。
_避免_：任务（范围过宽，无法明确是否包含实际签到执行）

**批量签到**：针对多个符合条件的签到账号统一发起签到批次的运营动作。
_避免_：批量任务（无法明确动作是创建批次还是执行签到）

**可执行账号**：在当前业务日期、账号状态和签到策略下，允许被纳入本次签到范围并执行签到的签到账号。
_避免_：有效账号（有效不等于当前允许签到）

**签到批次状态**：描述签到批次整体进展的状态，包括执行中、已完成、部分失败和全部失败；它不替代单个签到记录的状态。
_避免_：批次成功率（成功率是指标，不是批次的生命周期状态）

**批次汇总**：对一个签到批次的账号总数、完成数、成功数、失败数和跳过数进行的整体结果概览。
_避免_：批次结果（批次结果可能被误解为单个账号的签到记录）

**签到范围**：管理员确认批量签到时锁定的实际签到账号集合，同时包含因今日已签到、停用或其他条件而跳过的账号及原因。
_避免_：当前列表（当前列表可能在批次执行期间发生变化）

**批次可见范围**：某个用户在签到批次中允许查看的签到账号集合，按账号当前归属确定；它可以小于批次创建时锁定的完整签到范围，但不改变批次实际执行范围。
_避免_：批次所有权（创建人不等于批次中每个签到账号的归属人）

**签到记录**：签到批次中针对单个签到账号的一次签到执行及其结果所形成的业务记录。
_避免_：日志（日志是技术记录，不能替代业务记录）

**重试记录**：针对失败的签到记录再次执行后产生的新签到记录，不覆盖原始失败记录。
_避免_：重试状态（状态不能表达这次重新执行本身）

**今日已签到**：某个签到账号在当前业务日期内已经存在完成签到结果的状态判断，用于默认跳过重复签到。
_避免_：本次成功（今日已签到可能来自更早的签到执行，并不代表当前操作新成功）

**业务日期**：按照平台时区划分、用于判断今日已签到、每日尝试次数和运营统计归属的日期。
_避免_：自然日（自然日容易被理解为浏览器或服务器本地日期，不能明确平台口径）

**账号状态**：签到账号在签到运营中的当前可操作状态，包括待签到、签到中、成功、失败和已停用；余额预警不属于账号状态本身。
_避免_：健康状态（容易与服务器或站点健康混淆）

**余额预警**：签到账号的余额达到预设风险条件后产生的独立运营提醒。
_避免_：余额状态（余额数值和运营提醒是两个不同概念）

**可重试失败**：因站点暂时不可用、网络异常或其他可恢复原因导致、允许再次发起签到的失败结果。
_避免_：失败（没有表达是否允许再次执行）
