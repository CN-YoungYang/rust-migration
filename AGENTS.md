## Agent skills

### 问题跟踪器

问题和规格说明以 Markdown 文件形式保存在 `.scratch/<feature-slug>/` 下。详见 `docs/agents/issue-tracker.md`。

### 领域文档

单上下文领域文档使用根目录下的 `CONTEXT.md` 和 `docs/adr/`。详见 `docs/agents/domain.md`。
